def objects_counts(container):
    """
    method that counts how many objects exist in any given container

    """

    # assign how many objects in container
    object_count = len(container)

    return object_count